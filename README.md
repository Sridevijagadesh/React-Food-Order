#Created Parcel

- script;{
  "start": "parcel index.html",
  "build": "parcel build index.html"
  }

run
npm start index / npm start
npm build index

/ created react and react-dom
in the html page used <script type="module">

//Use state()-> change the UI state
//use Effect() -> Side effect of the API
//use Custome Hook -> Create our own hooh for
*Readable
*Modularity
\*Reusability

React- router
creatBrowersRoute RouterProvider Outlet
Path, element error child
Link navLink -- link this app. like achor tag

Life cycle of react
\*MOunting/ loading Update Unmounting
---constructor ---constructor -componentdidUpade
----render ---render
---compontent did Mounting ---compontent did Mounting

---lazy
---> React Function to slow doen the calling or when it on demant use lazy function

---suspended componenet
---> use suspended compentent with props called fallback as the function
An essential element of React Suspense is the Suspense component. It lets you declare how to handle fallback content while asynchronous actions are pending and encapsulate any portion of your component tree.
